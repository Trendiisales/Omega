name: verify-active

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install python
        run: |
          python3 --version

      - name: verify active include graph
        run: |
          python3 tools/VERIFY_ACTIVE.py
