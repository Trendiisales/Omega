# SUPERSEDED by backtest/honest_backtest_xauusd.cpp.
# This Python draft existed for ~5 minutes during S26 P1B session as a
# fast iteration draft; per operator preference the harness must be C++
# with CRTP to mirror microscalper_crtp_sweep.cpp. See the .cpp file in
# backtest/ for the actual implementation. This file kept as a tombstone
# rather than deleted because the workspace mount disallows shell rm.
raise SystemExit("deprecated: use backtest/honest_backtest_xauusd.cpp")
