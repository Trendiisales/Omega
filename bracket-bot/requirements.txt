# Omega gold-bracket bot - runtime dependencies (live execution + dashboard)
# Install:  pip install -r requirements.txt
#
# These two top-level packages pull everything the live bracket bots and the
# Flask dashboard need:
#   ib_insync -> eventkit, nest_asyncio
#   flask     -> werkzeug, jinja2, click
ib_insync==0.9.86
flask==3.1.3

# NOTE: the research/ backtests additionally need pandas, numpy, yfinance and
# vectorbt. Those are NOT required to run the live strategy on the VPS and are
# intentionally left out to keep the VPS environment minimal. Install them
# separately on a research machine if you want to re-run the backtests.
